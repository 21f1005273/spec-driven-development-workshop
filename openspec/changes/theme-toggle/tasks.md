## 1. CSS Theme Definitions

- [ ] 1.1 Add CSS variable overrides for `body.light-theme` class to redefine layout colors
- [ ] 1.2 Configure smooth CSS transition styling on color properties of main elements

## 2. Toggle UI Component

- [ ] 2.1 Add the theme toggle button inside the header bar using SVGs for sun/moon icons
- [ ] 2.2 Position and style the theme toggle button for dark/light themes

## 3. JS Logic and Storage Persistence

- [ ] 3.1 Implement inline script in index.html to check localStorage key `notes_theme` and apply theme class instantly on body
- [ ] 3.2 Attach click event listener to the toggle button to swap theme classes and update localStorage
- [ ] 3.3 Ensure the visual toggle icon updates dynamically to match the active theme

## 4. Testing and Verification

- [ ] 4.1 Confirm that theme transitions operate smoothly and contrast is acceptable in light mode
- [ ] 4.2 Check that reloading the page preserves the selected theme preference
